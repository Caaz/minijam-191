class_name GameplayUI extends Control

@export var score_label:Label
@export var time_label:Label

func ready() -> void:
	pass
	# This is where we'd connect to signals to update the score and time labels.
